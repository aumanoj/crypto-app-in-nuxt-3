export default defineAppConfig({
  ui: {
    primary: "orange",
    notifications: {
      // Show toasts at the top right of the screen
      position: "top-0 left-[50%] -translate-x-1/2 bottom-auto",
    },
  },
  nuxtIcon: {},
});
